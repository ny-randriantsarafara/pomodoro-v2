import 'package:flutter/widgets.dart';

class TopFocusAreas extends StatelessWidget {
  const TopFocusAreas({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
