import 'package:flutter/widgets.dart';

class AuthInputField extends StatelessWidget {
  const AuthInputField({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
