import 'package:flutter/widgets.dart';

class NextFocusHero extends StatelessWidget {
  const NextFocusHero({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
