import 'package:flutter/widgets.dart';

class RecoveryState extends StatelessWidget {
  const RecoveryState({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
