import 'package:flutter/widgets.dart';

class BreakTimerRing extends StatelessWidget {
  const BreakTimerRing({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
