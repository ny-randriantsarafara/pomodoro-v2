import 'package:flutter/widgets.dart';

class BreakActions extends StatelessWidget {
  const BreakActions({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
