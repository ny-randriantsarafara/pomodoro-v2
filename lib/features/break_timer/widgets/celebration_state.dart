import 'package:flutter/widgets.dart';

class CelebrationState extends StatelessWidget {
  const CelebrationState({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
